/** Beatmap difficulty info custom data interface for Contributor.
 * ```ts
 * _role: string,
 * _name: string,
 * _iconPath: string
 * ```
 */
export interface IContributor {
    _role: string;
    _name: string;
    _iconPath: string;
}
