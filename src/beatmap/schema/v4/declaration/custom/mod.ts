import * as info from './info.ts';

/** Schema for v4 `Custom Characteristic`. */
export const CustomCharacteristicSchema = /* @__PURE__ */ info.CustomCharacteristicSchema();
/** Schema for v4 `Info` custom data. */
export const CustomDataInfoSchema = /* @__PURE__ */ info.CustomDataInfoSchema();
/** Schema for v4 `Info Difficulty` custom data. */
export const CustomDataInfoBeatmapSchema = /* @__PURE__ */ info.CustomDataInfoBeatmapSchema();
